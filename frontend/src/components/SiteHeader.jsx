import { Box, Typography } from "@mui/material";

const SiteHeader = () => {

    return (
        <Box>
            <Typography variant='h3'>Pokemon Finder</Typography>
            <Typography variant="h5">El que quiere Pokemones, que los busque</Typography>
        </Box>
    )
}

export default SiteHeader;